<nav
  class="navbar navbar-expand-lg bg-dark text-light shadow-lg"
  data-bs-theme="dark"
>
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand text-primary fw-bold" href="/">BlogiFy</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- Links -->
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto">
        <!-- Home Link -->
        <li class="nav-item">
          <a class="nav-link text-light" aria-current="page" href="/">Home</a>
        </li>

        <% if (locals.user) { %>
        <!-- Add Blog Link -->
        <li class="nav-item">
          <a class="nav-link text-light" href="/blog/add-new">Add Blog</a>
        </li>
        <!-- User Dropdown -->
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle text-light"
            href="#"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <% users.forEach(element => { %> <% if
            (JSON.stringify(element._id) === JSON.stringify(user._id)) { %> <%= JSON.stringify(element.fullName) %> <% } %> <%
            }) %>
          </a>
          <ul class="dropdown-menu dropdown-menu-dark">
            <li>
              <a class="dropdown-item" href="/user/logout">Log Out</a>
            </li>
          </ul>
        </li>
        <% } else { %>
        <!-- Create Account Link -->
        <li class="nav-item">
          <a class="nav-link text-light" href="/user/signup">Create Account</a>
        </li>

        <!-- Sign In Link -->
        <li class="nav-item">
          <a class="nav-link text-light" href="/user/signin">Sign In</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
